node_modules/
dist/
.env
.DS_Store
*.log

# Local runtime data (SQLite links + refresh tokens) — never commit.
# Note: config/role-mappings.json IS committed (the demo gating config; edit it
# when you fork). Only the database stays out of git.
data/
